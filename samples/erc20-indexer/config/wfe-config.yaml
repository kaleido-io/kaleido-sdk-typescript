# Workflow Engine connection config.
# Set KALEIDO_CONFIG_FILE env var to point to this file (default: ./config/wfe-config.yaml).
#
# providerName must match the `name` exported from src/provider.ts.
# Workflow Engine connection config.
# Set KALEIDO_CONFIG_FILE env var to point to this file (default: ./config/wfe-config.yaml).
# providerName must match the `name` exported from src/provider.ts.
workflow-engine:
  providerName: erc20-indexer
  providerMetadata:
    displayName: ERC-20 Indexer
    description: Indexes ERC-20 Transfer events into the Kaleido Asset Manager
  url: https://dave.sbx2.devel.eap.labs.kaleido-demos.com/endpoint/ff-nested-errors-testing/workflow-engine/rest
  auth:
    type: basic
    username: assets
    password: 1242ac77-9a1c-4804-af92-a8fd2d2b38eb4cf02849-0412-43a0-be49-f2acb471eed1
  # retryDelay: 2s
  # maxRetries: 5   # omit for infinite retries
