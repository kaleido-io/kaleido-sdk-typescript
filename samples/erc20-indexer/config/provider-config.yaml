# Provider configuration for the ERC-20 Indexer.
# Set CONFIG_FILE env var to point to this file (default: ./config/provider-config.yaml).

# Asset Manager connection — where indexed transfers are written.
assetManager:
  account: dave.sbx2.devel.eap.labs.kaleido-demos.com
  environment: ff-nested-errors-testing
  serviceName: tokenization1-asset-manager
  auth:
    keyName: assets 
    keyValue: 1242ac77-9a1c-4804-af92-a8fd2d2b38eb4cf02849-0412-43a0-be49-f2acb471eed1 

# ERC-20 contract to index
erc20:
  contractAddress: '0x76755d67574781a55d35ba85af39511f4a929acd'
  contractName: erc20
  contractSymbol: TKN
  chain: besu          # Informational label attached to indexed transfers (e.g. "ethereum", "polygon", "base", "besu")

# Service ID of the EVM Connector provider (used when creating the event stream)
evmConnector: s-p2za8d90ae
