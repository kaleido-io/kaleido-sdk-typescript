# Sample workflow

Example workflow and transaction for the `hello` handler registered via `provider-config.json`.

```bash
npm run create-workflow snippet/flow.ts
npm run create-transaction snippet/transaction.ts
```
