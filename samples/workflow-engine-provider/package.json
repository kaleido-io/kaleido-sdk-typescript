{
    "name": "workflow-engine-provider",
    "version": "0.0.1",
    "scripts": {
        "prebuild": "npm run clean",
        "build": "tsc",
        "clean": "rm -rf dist",
        "start": "node dist/connect.js",
        "start:dev": "tsx ./connect.ts",
        "create-workflow": "tsx ./utils/post-workflow.ts",
        "create-transaction": "tsx ./utils/post-transaction.ts",
        "create-stream": "tsx ./utils/post-stream.ts",
        "test": "vitest run",
        "test:watch": "vitest watch",
        "test:coverage": "vitest run --coverage",
        "package:docker": "docker build --platform linux/amd64 -t workflow-engine-provider:latest .",
        "package:podman": "podman build --platform linux/amd64 -t workflow-engine-provider:latest .",
        "promote:docker": "docker tag workflow-engine-provider:latest ${ARTIFACT_REGISTRY}/workflow-engine-provider:${IMAGE_TAG} && docker push ${ARTIFACT_REGISTRY}/workflow-engine-provider:${IMAGE_TAG}",
        "promote:podman": "podman tag workflow-engine-provider:latest ${ARTIFACT_REGISTRY}/workflow-engine-provider:${IMAGE_TAG} && podman push ${ARTIFACT_REGISTRY}/workflow-engine-provider:${IMAGE_TAG}",
        "promote:crane": "crane copy ${SOURCE_REGISTRY}/workflow-engine-provider:${IMAGE_TAG} ${ARTIFACT_REGISTRY}/workflow-engine-provider:${IMAGE_TAG}",
        "promote:crane-local": "crane copy --insecure daemon://workflow-engine-provider:latest ${ARTIFACT_REGISTRY}/workflow-engine-provider:${IMAGE_TAG}",
        "promote:kind-local": "docker tag workflow-engine-provider:latest localhost:6001/workflow-engine-provider:${IMAGE_TAG} && docker push localhost:6001/workflow-engine-provider:${IMAGE_TAG}"
    },
    "private": true,
    "devDependencies": {
        "@types/js-yaml": "^4.0.9",
        "@vitest/coverage-v8": "^4.0.16",
        "tsx": "^4.21.0",
        "typescript": "^5.9.3",
        "vitest": "^4.0.16"
    },
    "type": "module",
    "main": "dist/connect.js",
    "files": [
        "dist",
        "README.md"
    ],
    "dependencies": {
        "@kaleido-io/core-sdk": "^1.0.0",
        "@kaleido-io/workflow-engine-sdk": "^1.0.0",
        "@kaleido-io/asset-manager-sdk": "^1.0.0",
        "dotenv": "^17.2.3",
        "js-yaml": "^4.1.1",
        "web3": "^4.16.0",
        "ws": "^8.18.0"
    }
}
