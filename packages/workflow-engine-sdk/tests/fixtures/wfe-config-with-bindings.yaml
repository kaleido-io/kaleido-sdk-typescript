# Fixture for service bindings tests
workflow-engine:
  providerName: test-provider
  url: http://localhost:5503
  auth:
    type: token
    token: dev-token-123
    header: X-Kld-Authz
    scheme: ""
  retryDelay: 2s

service-bindings:
  asset-manager:
    type: asset-manager
    bindingType: non-hosted
    url: https://example.com/endpoint/env/am/rest/api/v1
    auth:
      type: basic
      username: test-key
      password: test-secret
    maxRetries: 5
    timeout: 60000
  key-manager:
    type: key-manager
    bindingType: non-hosted
    url: https://example.com/endpoint/env/km/rest/api/v1
    auth:
      type: token
      token: km-token-456
      header: Authorization
      scheme: Bearer
  hosted-service:
    type: asset-manager
    bindingType: hosted
    id: u:9999
  no-url-binding:
    type: custom-service
