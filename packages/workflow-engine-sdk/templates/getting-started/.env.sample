# Copy this file to .env and fill in your values.
# .env is gitignored — never commit real credentials.

# API key value for the http-invoke sample (header name is set in config/config.yaml)
API_KEY=your-api-key-here
