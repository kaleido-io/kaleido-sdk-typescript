{
  "name": "kaleido-sdk-typescript",
  "version": "1.0.0",
  "private": true,
  "description": "Kaleido TypeScript SDKs",
  "engines": {
    "node": ">=22",
    "npm": ">=11"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kaleido-io/kaleido-sdk-typescript.git"
  },
  "author": "Kaleido",
  "license": "Apache-2.0",
  "keywords": [
    "kaleido",
    "sdk",
    "typescript",
    "blockchain",
    "evm",
    "bitcoin",
    "canton",
    "workflows",
    "workflow-engine"
  ],
  "workspaces": [
    "packages/*",
    "samples/*"
  ],
  "scripts": {
    "build:packages": "npm run build -w packages/core && npm run build -w packages/asset-manager -w packages/workflow-engine-sdk",
    "build:samples": "npm run build -w samples/getting-started -w samples/erc20-indexer -w samples/btc-indexer -w samples/bulk-upsert-sample -w samples/dependency-ordering-sample --if-present",
    "build": "npm run build:packages && npm run build:samples",
    "test:packages": "npm test -w packages/core -w packages/asset-manager -w packages/workflow-engine-sdk",
    "test:samples": "npm test -w samples/getting-started -w samples/erc20-indexer -w samples/btc-indexer",
    "test": "npm run test:packages && npm run test:samples",
    "lint": "npm run lint -w packages/workflow-engine-sdk"
  }
}
