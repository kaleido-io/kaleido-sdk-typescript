name: Test wesdk init

on:
  workflow_call:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  init:
    name: wesdk init --template ${{ matrix.template }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        template: [getting-started, erc20-indexer, btc-indexer]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22.x'

      - name: Install workspace dependencies
        run: npm install

      - name: Build packages
        run: npm run build:packages

      - name: Pack workflow-engine-sdk
        run: npm pack
        working-directory: packages/workflow-engine-sdk

      - name: Run wesdk init from tarball
        run: |
          TARBALL=$(ls "$GITHUB_WORKSPACE"/packages/workflow-engine-sdk/kaleido-io-workflow-engine-sdk-*.tgz)
          mkdir -p /tmp/wesdk-init-test
          cd /tmp/wesdk-init-test
          WESDK_REPO_URL="$GITHUB_WORKSPACE" npx "$TARBALL" init my-${{ matrix.template }} --template ${{ matrix.template }}

      - name: Verify generated project
        run: |
          PROJECT=/tmp/wesdk-init-test/my-${{ matrix.template }}
          echo "--- Checking structure ---"
          test -f "$PROJECT/package.json" && echo "package.json OK"
          test -f "$PROJECT/tsconfig.json" && echo "tsconfig.json OK"
          test -d "$PROJECT/src" && echo "src/ OK"
          test ! -f "$PROJECT/Dockerfile.withsdk" && echo "Dockerfile.withsdk correctly absent"
          echo "--- Checking version pinning ---"
          node -e "
            const pkg = JSON.parse(require('fs').readFileSync('$PROJECT/package.json', 'utf-8'));
            const allDeps = { ...pkg.dependencies, ...pkg.devDependencies, ...pkg.peerDependencies };
            let failed = false;
            for (const [dep, ver] of Object.entries(allDeps)) {
              if (!dep.startsWith('@kaleido-io/')) continue;
              if (ver === '*') {
                console.error('FAIL: ' + dep + ' is still unpinned (*)');
                failed = true;
              } else {
                console.log('OK:   ' + dep + ' => ' + ver);
              }
            }
            if (failed) process.exit(1);
          "

  init-add:
    name: wesdk init --template ${{ matrix.template }} (add to existing)
    runs-on: ubuntu-latest
    strategy:
      matrix:
        template: [getting-started, erc20-indexer, btc-indexer]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22.x'

      - name: Install workspace dependencies
        run: npm install

      - name: Build packages
        run: npm run build:packages

      - name: Pack workflow-engine-sdk
        run: npm pack
        working-directory: packages/workflow-engine-sdk

      - name: Create minimal existing project
        run: |
          mkdir -p /tmp/wesdk-add-test
          echo '{"name":"existing-project","version":"1.0.0","type":"module","dependencies":{}}' \
            > /tmp/wesdk-add-test/package.json

      - name: Run wesdk init in add-to-existing mode
        run: |
          TARBALL=$(ls "$GITHUB_WORKSPACE"/packages/workflow-engine-sdk/kaleido-io-workflow-engine-sdk-*.tgz)
          cd /tmp/wesdk-add-test
          WESDK_REPO_URL="$GITHUB_WORKSPACE" npx "$TARBALL" init --template ${{ matrix.template }}

      - name: Verify add-to-existing result
        run: |
          PROJECT=/tmp/wesdk-add-test
          echo "--- Checking src/ was copied ---"
          test -d "$PROJECT/src" && echo "src/ OK"
          echo "--- Checking root files were NOT copied ---"
          test ! -f "$PROJECT/tsconfig.json" && echo "tsconfig.json correctly absent"
          test ! -f "$PROJECT/Dockerfile"     && echo "Dockerfile correctly absent"
          test ! -f "$PROJECT/.gitignore"     && echo ".gitignore correctly absent"
          echo "--- Checking deps were merged and pinned ---"
          node -e "
            const pkg = JSON.parse(require('fs').readFileSync('$PROJECT/package.json', 'utf-8'));
            const allDeps = { ...pkg.dependencies, ...pkg.devDependencies };
            let found = false, failed = false;
            for (const [dep, ver] of Object.entries(allDeps)) {
              if (!dep.startsWith('@kaleido-io/')) continue;
              found = true;
              if (ver === '*') { console.error('FAIL: ' + dep + ' still unpinned'); failed = true; }
              else console.log('OK:   ' + dep + ' => ' + ver);
            }
            if (!found) { console.error('FAIL: no @kaleido-io/* deps were added'); failed = true; }
            if (failed) process.exit(1);
          "
