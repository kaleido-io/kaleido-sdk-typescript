name: Kaleido Workflow Engine TypeScript SDK CI

on:
  pull_request:
    paths:
      - 'packages/**'
      - 'internal/**'
      - 'samples/**'
      - '.github/workflows/ci-workflow-engine-sdk.yaml'
      - '.github/workflows/ci-docker-samples.yaml'
      - '.github/workflows/ci-ksdk-init.yaml'

permissions:
  contents: read

jobs:
  packages:
    name: Lint, Build & Test (packages)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22.x'

      - name: Install workspace dependencies
        run: npm install

      - run: npm run lint -w packages/workflow-engine-sdk

      - name: Build packages (dependency order)
        run: npm run build:packages

      - run: npm test -w packages/asset-manager-sdk -w packages/workflow-engine-sdk

  samples:
    name: Build & Test (samples)
    runs-on: ubuntu-latest
    needs: packages
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22.x'

      - name: Install workspace dependencies
        run: npm install

      - name: Build packages (required by samples)
        run: npm run build:packages

      - name: Build samples
        run: npm run build:samples

      - name: Test samples
        run: npm test -w samples/workflow-engine-provider -w samples/erc20-indexer -w samples/btc-indexer -w samples/canton-cip56-indexer --if-present

  docker:
    name: Docker Builds (Samples)
    needs: packages
    uses: ./.github/workflows/ci-docker-samples.yaml

  ksdk-init:
    name: ksdk init (templates)
    needs: packages
    uses: ./.github/workflows/ci-ksdk-init.yaml
